use aero::aio::{Aero,Next,Context};
// // use aero::Context;
// // use aero::Next;

fn main() {
    // let mut app = Aero::new("127.0.0.1:3000");

    // app.get("/",  Box::new(|ctx: &mut Context, next: Next | {
    //     Box::pin(async move {
    //         time::sleep(Duration::from_secs(1)).await;
    //         println!("--- go there1");
    //         ctx.body = "hello1".to_string();
    //         // println!("asdasd -----{:?}", Instant::now());
    //         next(ctx).await;
    //     })
    // }) );

    // // app.get("/hello", Box::new(|ctx: &mut Context, next: Next| Box::pin(async move {
    // //     ctx.send_text("Hello, world!");
    // // })));

    // // app.get("/hello", Box::new(|ctx: &mut Context, next: Next| Box::pin(async {
    // //     ctx.send_text("Hello, world!");
    // // })));

    // println!("Listening on http://{}", app.socket_addr);

    // tokio::runtime::Builder::new_multi_thread()
    //     // .worker_threads(6)
    //     .enable_all()
    //     .build()
    //     .unwrap()
    //     .block_on(app.run());
}


// pub fn main(){

// }